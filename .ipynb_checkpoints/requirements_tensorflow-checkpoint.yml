name: paml_env_tensorflow
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  # - tensorflow=2.15
  - matplotlib
  - ipykernel
  - pip
  - pip:
      - tensorflow==2.15 # Installing TensorFlow with Anaconda doesn't work for some reason